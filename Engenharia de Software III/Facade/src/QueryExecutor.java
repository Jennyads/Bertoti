// Subsistema para execução de consultas
class QueryExecutor {
    public void executeQuery(String query) {
        System.out.println("Executando a consulta: " + query);
        // Lógica para executar a consulta
    }
}